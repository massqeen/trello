import styled from 'styled-components';

export const CustomDragLayerContainer = styled.div`
  position: fixed;
  z-index: 100;
  height: 100%;
  width: 300px;
  top: 0;
  left: 0;
  pointer-events: none;
`;
